import styled from '@emotion/styled'

const TextInput = styled.input({
  display: 'flex',
  flexGrow: 1,
  height: 35,
  maxHeight: 35,
  padding: '0 20px',
  fontWeight: 700,
  margin: 15
})

export default TextInput